--- beta11/common/lib/modules/fglrx/build_mod/make.sh.orig	2012-12-03 04:03:45.000000000 +0200
+++ beta11/common/lib/modules/fglrx/build_mod/make.sh	2012-12-07 02:23:26.467868194 +0200
@@ -204,6 +204,11 @@ cd $current_wd
 
 src_file=$linuxincludes/linux/version.h
 
+# (tmb) hack to detect uapi change in 3.7
+if [ ! -e $src_file ]; then
+src_file=$linuxincludes/generated/uapi/linux/version.h
+fi
+
 if [ ! -e $src_file ]; then
   echo "kernel includes at $linuxincludes not found or incomplete" | tee -a $logfile
   echo "file: $src_file"                                           | tee -a $logfile
